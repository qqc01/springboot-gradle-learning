	public rspName methodName(requestModelName requestModelVariableName) {
		reqName.Builder request = reqName.newBuilder();
		ProtoBufUtil.transform(requestModelVariableName, request);
		return blockingStubVariableName.methodName(request.build());
	}
